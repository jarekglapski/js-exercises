// Write a for-loop that will iterate from 0 to 15.
// For each iteration, it will check if the current number is odd or even, and print a message to the console.

for (var i = 0; i < 16; i++) {
    console.log(i + ((i % 2) ? " is odd" : " is even"));
}
/**
 * Initialize two variables: admin and name
 * Assign string root to the variable admin
 * Copy the value from admin to the variable name
 * Print to the console both values
 */

var admin, name;
admin = 'root';
name = admin;
//admin = 'test';
console.log(admin, name);